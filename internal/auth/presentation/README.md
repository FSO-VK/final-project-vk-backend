# Presentation

Этот слой выступает по сути адаптером, который предоставляет API
для взаимодействия с Application слоем микросервиса.

Реализовывать этот слой мы можем разными путями: через http, через gRPC или kafka.
